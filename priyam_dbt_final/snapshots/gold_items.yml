# snapshots/gold_items.yml   ← replace the entire file with this
version: 2

snapshots:
  - name: gold_items
    relation: source('source', 'items')   # ← EXACTLY this line
    config:
      target_schema: gold
      target_database: '{{ target.catalog }}'
      unique_key: id
      strategy: timestamp
      updated_at: updateDate